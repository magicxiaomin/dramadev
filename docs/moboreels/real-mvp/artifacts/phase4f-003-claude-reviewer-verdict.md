# PHASE4F-003 Claude Reviewer Verdict

Verdict: CHANGES_REQUESTED

Findings:
- Hard-stop scope is correctly fake-only across both planning notes (no real payment/auth/Facebook/backend/video/secrets/DNS), and matches the user's approval envelope and the existing review-gate-checks.mjs hard-stop rules (banned-dependency, no-backend-api-routes, no-real-*).
- User approval explicitly covers @playwright/test devDependency + package.json/pnpm-lock.yaml changes + a Chromium-only 390x844 fake-only spec + bounded CI/check integration; the architect plan (PHASE4F-002) names the same surface area (devDep, playwright.config.ts, tests/e2e/variant-b-p0-facebook.spec.ts, pr-checks.yml or acceptance/dramadev-p0.sh) and stays inside that envelope. Note @playwright/test is not in the banned-dependency list — adding it does not trip pnpm check:banned-deps.
- The P0 invariant is unambiguously expressible against the current code: test ids variant-b-watch, current-episode, watch-lock-state, free-playback-state, complete-episode-button, continue-next-episode, locked-boundary-placeholder, unlock-drawer, mock-unlock-episode-link, story-pass-secondary-link, pass-mock-return-link, watch-attribution-source, watch-unlocked-flag all exist in watch-stub.tsx and pass-stub.tsx and are stable. buildWatchEpisodeHref/buildPassHref/buildPassReturnHref preserve attribution and emit unlocked=1 only on the return leg, so URL-level assertions are sound.
- BLOCKER — fixture/route mismatch in PHASE4F-001: the feasibility plan recommends `/variant-b/watch/paper-crown-protocol?episode=1&source=facebook` with EP3 as the locked boundary, but `paper-crown-protocol` is marked `isStub: true` with `stubNote: 'Original static stub for secondary discovery previews only.'` (src/data/fixtures/shows.ts:267-293). PRIMARY_SHOW_ID is `midnight-lantern-oath` with freeEpisodes=5/firstLockedEpisode=6 (src/data/fixtures/shows.ts:3, 212-215). PHASE4F-002 corrects this to midnight-lantern-oath/EP6, but the two planning artifacts disagree and the implementation card must pin the canonical fixture. A spec written against the stub would not validate the P0 conversion path.
- BLOCKER — the implementation task spec does not exist yet. Both PHASE4F-001 and PHASE4F-002 explicitly recommend creating a separate implementation card; until that card is written with: pinned showId/episode via fixture constants (PRIMARY_SHOW_ID, freeEpisodes+1), exact URL assertion list including `unlocked=1` only on return, viewport 390x844, Chromium-only project, webServer wiring, retries=0-or-1, artifacts on failure only, no `--with-deps` install in CI without a documented runtime budget, and explicit non-goals (no Home redirect, no pre-lock prompts, no real infra) — there is no dispatchable spec.
- Free-chain coverage gap in PHASE4F-002 step 3: walking from EP1 through EP5 by clicking `complete-episode-button` then `continue-next-episode` is correct, but the spec should bind to fixture-derived bounds (`freeEpisodes`) rather than hard-coding "until EP6", so a fixture change loudly breaks the test instead of producing a silent off-by-one.
- Attribution preservation is partially specified: PHASE4F-002 asserts `source=facebook` survives free chain and unlock return, but does not require asserting it is preserved through the Story Pass round-trip (`pass-back-watch-link` and `pass-mock-return-link` both call buildWatchEpisodeHref/buildPassReturnHref with full attribution). The implementation card should add that assertion to lock in the PR #19 contract.
- Negative assertions are listed in PHASE4F-001 but not pinned in PHASE4F-002 acceptance criteria: no `/variant-b` Home redirect post-unlock, no unlock drawer visible before EP=freeEpisodes+1, no login/payment/Facebook copy on the P0 path, and no `unlocked=1` emission on free-chain hrefs. These are P0-defining and must appear in the spec.
- CI integration boundary is described but not decided: pr-checks.yml currently runs install/hard-stop/banned-deps/unit/lint/build; the architect plan says "add a gated P0 browser step after build" but does not specify whether the e2e step is required-to-merge or advisory-only on the first PR. With Playwright install cost, a soft-gate (run, upload artifacts, do not block merge) is the safer first landing — this must be made explicit.
- PR #19 base/branching strategy is unresolved: PHASE4F-002 acknowledges PR #19 (feat/phase4e-callback-query-contract) is OPEN+MERGEABLE+green but does not state whether the harness card should branch from PR #19 or wait for it to land. The harness will assert callback/query contract behavior that PR #19 introduces, so the implementation card must either land after PR #19 merges or rebase on it; otherwise the spec would fail on main.
- Rollback path is sound (remove devDep, config, spec, CI step → repo returns to current unit-only gate posture); existing Vitest/lint/build/banned-deps/hard-stop gates are untouched by either planning doc, which preserves the user's approval envelope.

Blockers:
- Resolve fixture target: pin the implementation card to PRIMARY_SHOW_ID (midnight-lantern-oath) with episode boundaries derived from `freeEpisodes` (=5) and `firstLockedEpisode` (=6); explicitly retire PHASE4F-001's paper-crown-protocol/EP3 example as not P0.
- Author the dispatchable implementation card before any coding dispatch, with: exact files (package.json, pnpm-lock.yaml, playwright.config.ts, tests/e2e/variant-b-p0-facebook.spec.ts, pr-checks.yml or acceptance/dramadev-p0.sh), Chromium-only project name p0-mobile-390x844, viewport 390x844, webServer config, retries/artifacts policy, no secrets, no `--with-deps` decision documented, and copy of all hard-stop boundaries.
- Decide PR #19 sequencing: either set the harness card to branch from PR #19's head or block dispatch until PR #19 merges to main; do not dispatch the harness against a base that does not yet have the callback/query contract.
- Decide CI gating posture: soft-gate (advisory, upload artifacts) for the first landing, with a follow-up card to promote to required-to-merge once CI runtime is observed.
- Add explicit acceptance assertions to the spec: (a) attribution `source=facebook` preserved across full free chain, locked entry, mock unlock return, AND Story Pass round-trip; (b) `unlocked=1` absent on all free-chain hrefs and present only on the return URL; (c) no unlock drawer visible while episode ≤ freeEpisodes; (d) no navigation to `/variant-b` Home at any step; (e) no login/payment/Facebook copy on the P0 path.

Recommended next safe task:
- Create the PHASE4F implementation card "PHASE4F-003: Implement fake-only Playwright Chromium P0 regression harness (midnight-lantern-oath, EP1→EP6, 390x844)" assigned to a frontend/test implementer, containing the resolved fixture target, the explicit acceptance assertions above, package/lock approval clause, soft-gate CI integration on first landing, PR #19 sequencing decision, rollback steps, and a hard-stop boundary copy. Do not dispatch implementation until that card is written and the PR #19 sequencing decision is captured in it.

Evidence:
- Read /tmp/phase4f-001.txt (feasibility, 107 lines) and /tmp/phase4f-002.txt (architecture, 85 lines).
- Read docs/moboreels/scene-flow-facebook-ad-conversion-prd.md (source of truth): §6 P0 flow, §7 routes, §8.3-8.6 locked/drawer/return, §11 P0 scope, §15 acceptance checklist.
- Read src/data/fixtures/shows.ts: PRIMARY_SHOW_ID=midnight-lantern-oath, freeEpisodes=5, firstLockedEpisode=6; paper-crown-protocol explicitly `isStub: true`.
- Read src/app/variant-b/watch/[showId]/watch-stub.tsx and src/app/variant-b/pass/pass-stub.tsx: confirmed test ids and link wiring (mock-unlock-episode-link → buildWatchEpisodeHref unlocked=1; story-pass-secondary-link → buildPassHref; pass-mock-return-link → buildPassReturnHref).
- Read src/lib/query-params.ts: confirmed unlocked=1 emitted only via unlocked:true builder branch; attribution preserved through appendAttributionParams in watch/pass/return builders.
- Read package.json: no @playwright/test, no e2e scripts, scripts are dev/build/lint/test:unit/check:banned-deps; pnpm 10.33.2; Next 14.2.35.
- Read .github/workflows/pr-checks.yml: install/review-gate/banned-deps/test:unit/lint/build only; no e2e step.
- Read acceptance/dramadev-p0.sh: mirrors PR check gates; no browser stage.
- Inspected .github/scripts/review-gate-checks.mjs: hard-stop content rules for payment/Facebook/auth/database/destructive ops/DNS/competitors; no rule that would flag @playwright/test.
- Inspected banned-dependency: blocks payment/auth/etc.; does not list Playwright.
- `gh pr view 19 --json ...`: state=OPEN, baseRefName=main, headRefName=feat/phase4e-callback-query-contract, mergeable=MERGEABLE, all 4 checks SUCCESS.
- `git status` / `git log --oneline -10` / `git branch --show-current`: clean tree on feat/phase4e-callback-query-contract; recent commits include `feat: add phase 4e callback query contract` (56d662a) and prior planning packages; no implementation files modified during review.

---
Claude session: 694e590c-3178-45f5-8a39-f919767ed1f0
