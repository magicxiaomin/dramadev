<!--
Product/requirements adviser gate generated by mandated Claude Code CLI for Kanban task t_740e2021.
Session: 65182716-cce2-4e31-86f2-46ad0d29d26b
Scope: docs-only / fake-only / additive planning.
-->

# Phase 5 P1 Q3 Product/Requirements Adviser Gate — mock balance display default

# 1. Verdict: APPROVE

# 2. Rationale

Drawer-only mock balance display is the *strictly conservative* answer to PRD §16 line 785. Approving it as the P1 planning default does not require human product input because:

- It does not pick a new product behavior — it codifies the status quo. The architecture gate already confirms the runtime renders balance/cost only inside `unlock-drawer-balance` and `unlock-drawer-cost` (`src/app/variant-b/watch/[showId]/watch-stub.tsx:329-335`); there is no pre-lock wallet/coin/recharge/Story Pass/subscription/login/payment-like surface today.
- It is fully redundant with existing PRD requirements already approved by product: §8.4 requires balance+cost inside the Unlock Drawer; §3 Non-Goals forbids recharge or Story Pass promotion before the first lock; §5 mandates free-preview-first and transparent unlock only at the lock point; §6 P0 flow makes the drawer the unlock decision surface.
- The riskier alternative (a passive pre-lock balance chip) is the one that needs human product input. That path is *not* being approved here and remains a separately scoped, blocked decision.
- The decision stays inside the allowed envelope: fake-only, docs-only/additive planning, no PRD/roadmap promotion, no runtime/test/CI/fixture/route/lock/type/CTA-hierarchy changes required, P0 invariant preserved verbatim, no real payment/subscription/login/Facebook/analytics/backend/database/entitlement/video/deploy/DNS/secrets/NovelHub infra touched, no brand-significant copy decided (only neutral "mock"/"fake" framing).
- Both open PRs (#27 head `1874f65`, #28 head `15f0cc8`) and the Q2 first-lock hook surface are untouched by this verdict.

User input is **not** needed now. Re-engage product only if (a) a pre-lock balance-chip experiment is later proposed, (b) drawer copy moves beyond neutral mock framing into brand-significant wording, or (c) any hard-stop gate is approached.

# 3. Approved bounded decision

For Phase 5 P1 Q3, the mock balance is displayed **only inside the Unlock Drawer** at the first locked episode, sourced from `SceneFlowShow.mockBalance` / `mockCostPerEpisode` fixtures, rendered at `data-testid="unlock-drawer-balance"` and `data-testid="unlock-drawer-cost"`. Pre-lock surfaces (Watch chrome, header, playback area, episode-complete CTA, Episode Sheet header) show **no** balance, wallet, coin counter, recharge entry, Story Pass prompt, subscription prompt, login prompt, or payment-like CTA. The Episode Sheet locked-row cost-as-unlock-condition helper (`src/lib/episode-sheet.ts:32`) is preserved unchanged per PRD §8.2. Copy refinements, if any, stay neutral mock framing (e.g., "Mock balance", "Mock cost") and are bounded to the drawer; no marketing or legal-significant phrasing is approved.

# 4. P0 invariant preservation checklist

P0 invariant, exact:

```
/variant-b/watch/[showId]?episode=1&source=facebook -> free episode chain -> first locked episode -> Unlock Drawer -> fake unlock/pass -> same episode with unlocked=1
```

- [x] Ad landing route shape and `?episode=1&source=facebook` attribution unchanged.
- [x] `freeEpisodes` continues to set the per-drama lock point; lock semantics in `src/lib/lock.ts` unchanged.
- [x] Free preview remains login-free, recharge-free, subscription-free, Story-Pass-free, PWA-prompt-free, balance-education-free, and payment-like-CTA-free before the first locked episode.
- [x] First locked episode auto-opens the Unlock Drawer; drawer close leaves the page locked; locked playback tap reopens drawer.
- [x] Balance and cost render only inside `unlock-drawer-balance` and `unlock-drawer-cost`; never in header, chrome, Episode Sheet header, playback overlay, or episode-complete CTA.
- [x] Drawer CTA hierarchy unchanged: primary `Unlock EP X` / `Get coins to unlock` (balance-conditional, drawer-only), secondary `Get Story Pass`, tertiary `Maybe later`. Story Pass never becomes primary at first lock.
- [x] Fake unlock and Pass return land on the same episode with `unlocked=1` via `buildWatchEpisodeHref` / `buildPassReturnHref`; no Home redirect, no episode loss, no attribution drop.
- [x] Fake balance and `unlocked=1` remain review/demo signals only — not entitlement, account, wallet ledger, purchase history, or production pricing.

# 5. Non-scope / hard-stop confirmation

This verdict does **not** authorize, and explicitly blocks:

- Any pre-lock balance chip, wallet chip, coin counter, recharge entry, Story Pass prompt, subscription prompt, login prompt, payment-like CTA, "Get coins"/"Top up" affordance, or balance education before the first locked episode.
- Real wallet, real coin packs, real recharge, real payment, real subscription billing, real Story Pass billing, real login, real account/entitlement state, real wallet ledger, real backend/database/content API, real video provider, production analytics, Pixel/CAPI, Facebook API, production deploy, DNS/cutover, secrets, NovelHub production infrastructure.
- Legal/compliance, pricing/tax/local-price, refund/cancellation/renewal, or brand-significant monetization or copy decisions.
- Licensed, competitor, stock, third-party, or public-domain assets/copy without separate human/legal/product approval.
- PRD or roadmap promotion/edits; prototype-b-spec edits; promotion of the P1 decision brief to a confirmed product decision beyond Q3-drawer-only.
- Route/attribution shape changes (`src/lib/query-params.ts`), lock semantics changes (`src/lib/lock.ts`), Episode Sheet locked-row helper removal (`src/lib/episode-sheet.ts:32`), CTA wiring/test-id/hierarchy changes inside the drawer, type-shape additions on `SceneFlowEpisode`/`SceneFlowShow` beyond existing optional fields, or any new query params, cookies, storage, or network calls.
- Playwright/CI/lockfile/package/browser-install changes; Q2 first-lock hook work on PR #28 and PR #27 manual-continue regression must remain undisturbed.

# 6. Exact next task graph

User input is **not** required to start. Tasks are dependency-ordered; editorial/copy note is folded into requirements as neutral mock framing only.

1. **dramadev-requirements** (run by PM via Claude Code CLI; docs-only/fake-only/additive) — Narrow the Q3 decision packet + architecture gate into a single implementation-ready acceptance note for Q3 drawer-only mock balance. Must: quote the P0 invariant verbatim; list the pre-lock absence requirements and the drawer balance/cost presence requirements; include a small editorial sub-note approving only neutral mock-framing copy (e.g., "Mock balance", "Mock cost") and explicitly deferring brand-significant phrasing; restate the hard-stops in §5 above; reference the existing `unlock-drawer-balance` / `unlock-drawer-cost` test IDs as stable; reference fixtures `mockBalance` / `mockCostPerEpisode` in `src/data/fixtures/shows.ts`; declare additive-only scope. No PRD edit, no roadmap edit.

2. **dramadev-dev** (gated on 1; additive/fake-only) — Optional, only if requirements identifies a gap: tune `mockBalance` / `mockCostPerEpisode` in `src/data/fixtures/shows.ts` so the drawer demonstrably exercises both `Unlock EP X` and `Get coins to unlock` branches; optionally refine drawer balance/cost label copy in `watch-stub.tsx:329-335` without changing test IDs. No changes to `src/lib/query-params.ts`, `src/lib/lock.ts`, `src/lib/episode-sheet.ts`, `SceneFlowEpisode`/`SceneFlowShow` shape, free episode counts, story hooks, CTA wiring/order/test-IDs, or the Watch page chrome. Must avoid conflicts with PRs #27 (`1874f65`) and #28 (`15f0cc8`); branch from a base that does not disturb those heads.

3. **dramadev-qa** (gated on 2; additive/fake-only) — Manual walkthrough of P0 path on `midnight-lantern-oath` from `/variant-b/watch/midnight-lantern-oath?episode=1&source=facebook` through every free episode to the first locked episode and drawer, including: (a) pre-lock absence checks on EP1 and the last free episode (no balance/wallet/coin/recharge/Story Pass/subscription/login/payment-like UI in header, body, episode-complete CTA, or Episode Sheet header); (b) Episode Sheet locked-row cost helper preserved; (c) drawer auto-opens; balance and cost render inside drawer only; (d) both sufficient and insufficient balance CTA states verified; (e) fake unlock returns to same episode with `unlocked=1` and `source=facebook` preserved; (f) Pass route round-trip returns to same locked episode with `unlocked=1`; (g) drawer close + reopen via locked-playback tap. Run existing `vitest` suites under `src/lib/*.test.ts` green, including the `1874f65` manual-continue regression; if a pure helper was added in step 2, add a co-located `*.test.ts`. Capture local-only fake evidence screenshots (EP1 free, last free episode, first-lock drawer, post-unlock same-episode return) under `qa-evidence/`, labelled fake/local.

4. **dramadev-reviewer** (gated on 2 + 3; run by PM via Claude Code CLI) — Verify: P0 invariant preserved verbatim; no hard-stop scope crossed; no PRD/roadmap/prototype-b-spec edits; no real payment/subscription/login/Facebook/analytics/backend/database/entitlement/video/deploy/DNS/secrets/NovelHub infra dependencies introduced; no pre-lock balance/wallet/coin/recharge/Story Pass/subscription/login/payment-like UI added; drawer test IDs stable; CTA hierarchy intact; copy stays neutral mock framing; evidence labelled fake-only; no merge conflicts with PR #27 / PR #28 surfaces.

5. **dramadev-ops** (gated on 4) — Package the additive change set as a single PR off `main` (or a non-conflicting base), with title/body referencing the decision packet and architecture gate, marked docs-only/fake-only/additive, and explicit non-scope statement mirroring §5 above. Do not touch CI config, lockfile, package, or browser-install layers. Do not merge PR #27 or PR #28 from this task. Do not deploy.

Hard-stop holdback (carried forward unchanged): real wallet/coin packs/recharge/payment/subscription/Story Pass billing/login/account/entitlement, real backend/database/content API, production analytics, Pixel/CAPI, Facebook API, production deploy/DNS/secrets, licensed/competitor/uncleared assets, legal/compliance/pricing/tax/refund/cancellation/renewal decisions, NovelHub production infrastructure, pre-lock balance-chip experiment, and PRD/roadmap promotion remain blocked behind separate explicit human approvals.

---

## Claude Code run metadata

- JSON artifact: `/root/.hermes/profiles/dramapm/workspace/dramadev-phase5p1/q3-product-adviser-claude.json`
- Session: `65182716-cce2-4e31-86f2-46ad0d29d26b`
- Subtype: `success`
- is_error: `False`
- terminal_reason: `completed`
- num_turns: `3`
