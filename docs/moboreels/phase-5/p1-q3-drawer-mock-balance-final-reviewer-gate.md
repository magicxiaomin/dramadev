<!--
Final reviewer gate generated by mandated Claude Code CLI for Kanban task t_b70dfcc5.
Session: d3ab77a6-62eb-43ae-9aae-3437d4462a6f
Scope: fake-only / local evidence / no deploy.
JSON artifacts:
- /root/.hermes/profiles/dramapm/workspace/dramadev-phase5p1/t_b70dfcc5-claude-review.json
- /root/.hermes/profiles/dramapm/workspace/dramadev-phase5p1/t_b70dfcc5-claude-review-final.json
-->

# PHASE5P1-Q3-008 Reviewer Verdict: APPROVE

## Evidence inspected
- PR #29 metadata via `gh pr view 29`: head `50fd8631cc957383eee33548d51fa4f6d8152f59` (matches expected), base `docs/phase5p1-q2-first-lock-hook-gate`, `MERGEABLE`, 3 files changed (+20/-2).
- `gh pr diff 29`: exactly three files — `src/app/variant-b/watch/[showId]/watch-stub.tsx` (+3/-2, one new import + two `<p class="text-scene-muted">` label swaps), `src/lib/drawer-mock-balance.ts` (new, 7 lines, two pure zero-arg constant returners), `src/lib/drawer-mock-balance.test.ts` (new, 10 lines).
- Full read of `src/app/variant-b/watch/[showId]/watch-stub.tsx` (lines 1–372): `formatMockDrawerBalanceLabel()` and `formatMockDrawerCostLabel()` are referenced only inside the `{lockState === 'locked' && isUnlockDrawerOpen ? …}` drawer block (lines 309–369), specifically at lines 331 and 335 inside `data-testid="unlock-drawer-balance"` and `unlock-drawer-cost`. No new balance/wallet/coin/recharge text in header, free-playback block, locked-boundary-placeholder, episode-complete CTA, Episode Sheet, or route-metadata tiles.
- Source-of-truth and gate artifacts: `scene-flow-facebook-ad-conversion-prd.md` not touched (`git diff main...50fd8631 -- …` empty); `p1-q3-mock-balance-display-{product-adviser,architecture,decision-packet,requirements-acceptance}.md` and `p1-q3-drawer-mock-balance-claude-review.md` all consistent with the diff.
- QA report (`qa-evidence/phase5p1-q3/report.md`) and assertion JSON: verdict PASS at 390x844 chromium mobile; 10 screenshots covering EP1/EP5 pre-lock absence, EP6 first-lock drawer with `Drawer mock balance` / `80 coins` and `Drawer mock cost` / `36 coins`, drawer close/reopen, fake unlock same-episode return with `source=facebook&unlocked=1`, Story Pass round trip, and direct EP7 later-locked drawer-only. `blockedRequests: []`, `consoleMessages: []`.
- Local commands run: `pnpm vitest run src/lib/drawer-mock-balance.test.ts` → 1 passed. `git diff main...50fd8631` against PRD/roadmap/prototype-b-spec, package.json/pnpm-lock.yaml, `.github/**`, `playwright.config*` → all empty.
- PR #27 (`1874f65`) and PR #28 (`15f0cc8`) both `MERGEABLE`, `OPEN`, untouched by this PR's surface; PR #29 is based on PR #28's branch so first-lock-hook surfaces are inherited, not modified.
- CI on PR #29: `apply metadata labels only` pass, `deterministic review gate` pass, `lint, unit, build, and hard-stop checks` pass.

## P0 invariant and scope findings
- P0 invariant preserved verbatim. No edits to `parseWatchQueryParams`, `getEpisodeLockState`, `buildWatchEpisodeHref`, `buildPassHref`, the drawer auto-open `useEffect`, locked-boundary-placeholder, locked-playback reopen path, or attribution handling. `unlockHref` (line 43) still passes `unlocked: true`; QA evidence #07 confirms post-unlock URL `/variant-b/watch/midnight-lantern-oath?episode=6&source=facebook&unlocked=1`, and #09 confirms the Pass round trip lands on the same episode with `unlocked=1`. No Home redirect, no episode loss.
- Drawer-only constraint satisfied. Both new helpers fire only inside the `lockState === 'locked' && isUnlockDrawerOpen` guard (line 309). The pre-lock chain (EP1, EP5 free; Episode Sheet header) shows no balance/wallet/coin/recharge/Story Pass/subscription/login/payment-like UI per QA screenshots 01, 02, 03 and source read.
- Test IDs stable and drawer-only: `data-testid="unlock-drawer-balance"` (line 330) and `data-testid="unlock-drawer-cost"` (line 334) unchanged in structure and location; coin values and surrounding markup untouched.
- CTA hierarchy intact: primary `Unlock EP X` / `Get coins to unlock` (line 350, balance-conditional, drawer-only), secondary `Get Story Pass (mock)` (line 357), tertiary `Maybe later` (line 365). Story Pass is not primary at first lock. Confirmed in QA screenshot 04 (`Unlock EP 6` primary).
- Episode Sheet locked-row cost-as-unlock-condition helper preserved per PRD §8.2 (QA screenshot 02 shows `36 coins` on EP6 locked row).
- Same-episode unlock/pass return with `source=facebook&unlocked=1` preserved (QA #07, #09).
- No conflict with PR #27 (manual continue) or PR #28 (first-lock hook); branch is based on PR #28's head and adds only the helper, test, and two label swaps.

## Hard-stop findings
- No production deploy, DNS/cutover, secrets, or environment changes.
- No real payment, subscription, Story Pass billing, coin packs, wallet ledger, account/entitlement, login, Facebook API, Pixel/CAPI, production analytics, backend/database/content API, or video provider touched. Helpers return string literals only; no I/O, no env access, no network.
- No legal/compliance, pricing/tax/local-price, refund/cancellation/renewal, or brand-significant monetization wording introduced. `Drawer mock balance` / `Drawer mock cost` are explicit, neutral mock framing that cannot be mistaken for real wallet/account state, satisfying requirements §7.
- No PRD, roadmap, or prototype-b-spec edits (verified via `git diff main...50fd8631` for those paths).
- Evidence is fake/local only: dev server on `127.0.0.1:4175`, `blockedRequests: []`, `consoleMessages: []`, screenshots labelled fake/local, no production device or analytics implied.
- No licensed/competitor/stock/third-party assets; labels are original placeholder strings.
- No NovelHub production infrastructure touched.
- No `package.json`, `pnpm-lock.yaml`, `.github/**`, or `playwright.config*` changes.

## QA caveat assessment
- Caveat: insufficient-balance branch (`Get coins to unlock` at line 350) exists in the source conditional governed by `hasEnoughCoins = show.mockBalance >= show.mockCostPerEpisode` (line 50) but is not route-selectable; default fixture is `80 >= 36`, so QA exercised only the sufficient-balance branch.
- Non-blocking for this PR. (1) The PR is a copy-only change to two `<p>` labels above the CTA, and does not touch `hasEnoughCoins`, the CTA `<Link>`, or `unlockHref`; the new helpers are constant strings independent of balance state, so behavior on the insufficient branch is provably identical to behavior on the sufficient branch w.r.t. drawer labels. (2) The branch lives inside the same drawer guard, so it cannot leak balance UI pre-lock regardless of which branch fires. (3) Requirements acceptance §9 explicitly marks fixture-level balance tuning to exercise both branches as optional ("Work is optional — start only if requirements/QA later identifies a gap"). (4) Architecture gate and product adviser gate both treat the insufficient-branch label as drawer-only and unchanged by Q3 scope.
- The caveat is worth carrying forward as a non-blocking follow-up for a later QA pass that tunes `mockBalance` / `mockCostPerEpisode` in `src/data/fixtures/shows.ts` to demonstrably exercise the `Get coins to unlock` branch, per requirements §9.

## Required follow-ups
- None blocking. PR #29 may proceed to ops packaging (P1 adviser task 5) without code changes.
- Non-blocking, optional, for a separately scoped future task only:
  - Tune `mockBalance` / `mockCostPerEpisode` in `src/data/fixtures/shows.ts` on a non-primary fixture so the `Get coins to unlock` drawer branch is route-selectable for QA, without disturbing the `midnight-lantern-oath` P0 path. Must remain fake/original/non-real-pricing and stay drawer-only.
  - Ops packaging should reference this verdict, the requirements acceptance note, the architecture gate, the decision packet, and the prior dev review artifact; mark the PR docs-only/fake-only/additive with explicit non-scope mirroring requirements §8.
  - When PR #28 (base branch) merges, confirm PR #29 retargets `main` cleanly with no conflicts on `watch-stub.tsx` or `src/lib/*`.
