<!--
Architecture gate generated by mandated Claude Code CLI for Kanban task t_1eb62556.
Session: 5026dcf7-f1b8-4953-95c3-e719e5586dea
Scope: docs-only / fake-only / additive planning.
-->

# Phase 5 P1 Q2 Architecture Scope Gate — first-lock hook default

Status: ARCHITECTURE GATE APPROVE for docs-only / fake-only / additive planning only. Product/editorial confirmation remains pending.

## Decision
- Approve the first-lock-only custom story hook proposal for packaging as a docs-only architecture/requirements gate artifact, not as a confirmed product decision. This authorization covers only docs-only / fake-only / additive planning and a later additive QA review if separately authorized. It explicitly does NOT authorize production deployment, real payment/subscription/login/Facebook API/analytics/backend/database/entitlement/video infrastructure, secrets, DNS/cutover, licensed/competitor/uncleared assets, legal/compliance/brand-significant copy decisions, or NovelHub production infrastructure. Human product confirmation on first-lock-only coverage, copy tone, and fallback wording remains an open gate.

## Architecture scope boundary
- Route boundary: All Q2 work stays inside the existing `/variant-b/watch/[showId]` route surface and the existing `/variant-b/pass` mock surface. No new routes, no new query params, no changes to attribution param parsing in `src/lib/query-params.ts:85-94`. The Unlock Drawer remains rendered inline inside `src/app/variant-b/watch/[showId]/watch-stub.tsx:308-368`.
- State/data boundary: Hook content is read-only fixture data on `SceneFlowEpisode.storyHook` (`src/types/scene-flow.ts:3-8`) for shows in `src/data/fixtures/shows.ts`. No new state fields, no new types, no runtime mutation, no storage, no cookies, no network. `unlocked` remains a fake URL flag (`src/lib/lock.ts:13-19`); it must not become entitlement.
- UI/component boundary: Hook copy renders only inside `data-testid="unlock-drawer-hook"` (watch-stub.tsx:324-326) for the watch drawer and `data-testid="pass-story-hook"` for the pass stub (pass-stub.tsx:42-44). Locked boundary placeholder (watch-stub.tsx:114-132) may also surface the hook; the safe generic fallback already exists at watch-stub.tsx:325 (`'Unlock once and continue watching this episode.'`) and pass-stub.tsx:44 (`show.logline`).
- Test boundary: Any later authorized test must be additive under existing `src/lib/*.test.ts` (e.g., `episode-sheet.test.ts`, `lock.test.ts`, `query-params.test.ts`) plus possibly one new co-located unit test for a pure hook-resolution helper. No Playwright, no CI/lockfile/package/browser-install changes. Existing P0 manual-continue regression (`1874f65`) must remain green.
- Non-scope / hard stops: Real Facebook redirect/API/Pixel/CAPI, real auth/login placement, real payment/subscription/Story Pass billing, real entitlement/wallet ledger, real backend/database/content API, real video provider/playback, production analytics, production deploy/DNS/secrets, licensed/competitor/stock/third-party/public-domain assets, legal/compliance/brand sign-off, NovelHub production infrastructure, and Q9–Q14, Q16, Q19–Q23 from the P1 brief. Q2 also does not authorize promotion of the P1 brief from proposal to confirmed product decision, nor PRD or roadmap edits.

## P0 invariant preservation
- Invariant, quoted exactly:
  ```txt
  /variant-b/watch/[showId]?episode=1&source=facebook -> free episode chain -> first locked episode -> Unlock Drawer -> fake unlock/pass -> same episode with unlocked=1
  ```
- No-regression constraints:
  - Ad landing route shape, including `?episode=1&source=facebook`, is unchanged; attribution preservation in `appendAttributionParams` (query-params.ts:103-110) is untouched.
  - `freeEpisodes` continues to determine the lock point via `getEpisodeLockState` (lock.ts:13-19). No per-episode hook absence may move, weaken, or bypass the lock point.
  - First-lock detection still opens the Unlock Drawer (watch-stub.tsx:39, 78-83); drawer close still leaves the page locked; locked-playback tap still reopens the drawer (watch-stub.tsx:149-157).
  - CTA hierarchy stays: primary `Unlock EP X` (watch-stub.tsx:344-350), secondary `Get Story Pass` (watch-stub.tsx:351-357), tertiary Maybe later (watch-stub.tsx:358-365). Story Pass never becomes primary on the first lock.
  - Mock unlock and Pass return continue to land on the same episode with `unlocked=1` via `buildWatchEpisodeHref({..., unlocked: true})` (query-params.ts:112-121) and `buildPassReturnHref` (query-params.ts:138-140). No Home redirect, no episode loss, no attribution drop.
  - Balance/cost remain inside the drawer only (drawer-balance / drawer-cost test IDs), preserving Q3's free-preview-first rule; hook display does not introduce balance, recharge, login, PWA install, subscription, or payment-like prompts before first lock.
  - Locked state remains visually distinct from network/video error states (PRD §8.3); fallback hook copy may not mention errors, retries, or offline.

## Later implementation task boundaries
- Surfaces a later authorized dev MAY inspect/change (additive only, fake-only):
  - `src/data/fixtures/shows.ts`: add or refine synthetic `storyHook` on `episodeNumber === firstLockedEpisode` for any show. Hook copy must be original, non-licensed, non-competitor; tone gate still pending product confirmation. Later locked episodes may omit `storyHook` and rely on the existing generic fallback.
  - `src/app/variant-b/watch/[showId]/watch-stub.tsx:324-326` and `:118-119`: keep the existing `episode?.storyHook ?? '...generic fallback...'` pattern. May extract the fallback string to a tiny helper if it stays pure and unit-tested.
  - `src/app/variant-b/pass/pass-stub.tsx:42-44`: same fallback pattern; may align fallback string with the watch surface for consistency, but copy remains placeholder-only.
  - Optional: a new small pure helper (e.g., `resolveDrawerHook(episode, show)`) co-located under `src/lib/` with an additive `*.test.ts`. Must be pure, no side effects, no I/O.
- Things a later dev MUST NOT change:
  - `src/lib/query-params.ts` route/attribution shape, `buildWatchEpisodeHref`, `buildPassReturnHref`, `parseWatchQueryParams`, `appendAttributionParams`, attribution key set.
  - `src/lib/lock.ts` lock semantics or `getEpisodeLockState` return type.
  - `SceneFlowEpisode` / `SceneFlowShow` shape in `src/types/scene-flow.ts` beyond keeping `storyHook?: string` optional. No required new fields, no per-episode required hook field, no entitlement field.
  - `freeEpisodes`, `firstLockedEpisode`, `mockBalance`, `mockCostPerEpisode`, `episodeRanges`, totalEpisodes for `PRIMARY_SHOW_ID`.
  - CTA wiring, ordering, or test IDs in the Unlock Drawer (`unlock-drawer`, `mock-unlock-episode-link`, `story-pass-secondary-link`, `unlock-drawer-maybe-later`).
  - The locked boundary placeholder structure, drawer auto-open effect (watch-stub.tsx:78-83), and locked-playback reopen path.
  - PRD, roadmap, prototype-b-spec, package/lockfile, CI config, Playwright configuration, environment, secrets, deployment, DNS.

## Required validation for later QA/reviewer
- Read `docs/moboreels/phase-5/p1-q2-first-lock-hook-decision-packet.md` and confirm docs-only / fake-only / non-implementation status remains intact.
- With dev server local-only (no deploy), walk the P0 path from `/variant-b/watch/midnight-lantern-oath?episode=1&source=facebook` through EP1–EP5 free chain, into EP6 (first locked), confirming:
  - `data-testid="unlock-drawer"` opens automatically on first entry to EP6.
  - `data-testid="unlock-drawer-hook"` shows the EP6 custom hook from fixtures (not the generic fallback string).
  - `data-testid="unlock-drawer-cost"` and `unlock-drawer-balance` show fake values from the show fixture only.
  - Primary CTA reads `Unlock EP 6` (or `Get coins to unlock` if balance < cost), secondary reads `Get Story Pass (mock)`, tertiary `Maybe later`.
  - Following `mock-unlock-episode-link` returns to `/variant-b/watch/midnight-lantern-oath?episode=6&unlocked=1` preserving `source=facebook` and any attribution params.
  - Following `story-pass-secondary-link` then `pass-mock-return-link` lands on the same episode with `unlocked=1`.
- Navigate directly to a later locked episode (e.g., `?episode=10`) without `unlocked=1` and confirm the drawer opens, hook falls back to the generic safe copy, CTA hierarchy is unchanged, and no broken/blank/competitor/payment-implying text appears.
- Confirm hook fallback never shows error/retry/offline language and never appears for episodes ≤ `freeEpisodes`.
- Run existing `vitest` unit suites under `src/lib/*.test.ts` (lock, episode-sheet, query-params, callback-keys, episode-range) and confirm all green; the new manual-continue regression test from `1874f65` must remain green.
- Confirm no new network calls, no new env vars, no new imports of payment/auth/analytics/Facebook SDK/video providers, and no fixture content sourced from licensed/competitor/third-party material.
- Capture local-only fake evidence: one screenshot of first-lock drawer with custom hook at ~390x844, one screenshot of a later-lock drawer with fallback copy. Mark evidence as fake/local; no production device, deploy, or analytics implied.

## Recommended next Kanban tasks
1. Product owner task (blocking gate): confirm or reject first-lock-only as the P1 default per the P1 decision brief Q2; approve fallback-copy phrasing rule (single centralized phrase vs per-drama). No code starts until this returns.
2. Editorial/copy task (blocking gate, docs-only): approve synthetic first-lock hook tone guidelines and a single safe generic fallback phrase; confirm no licensed/competitor/uncleared text.
3. Dev task (gated on 1 + 2, additive, fake-only): if authorized, ensure `storyHook` is present on `episodeNumber === firstLockedEpisode` for `PRIMARY_SHOW_ID` and stub shows in `src/data/fixtures/shows.ts`; optionally extract the fallback constant. No type/route/lock changes.
4. QA task (gated on 3): additive fake-only regression that exercises first-lock hook visibility, later-lock fallback safety, and the full P0 route → drawer → unlock/pass → `unlocked=1` return on `midnight-lantern-oath`. Add to `src/lib/*.test.ts` only if a pure helper was introduced; otherwise manual walkthrough plus existing unit suites.
5. Reviewer task (gated on 3 + 4): verify P0 invariant preserved verbatim, no hard-stop scope crossed, no PRD/roadmap edits, no real-service dependencies introduced, and evidence labelled fake-only.
6. Hard-stop holdback: every-locked-episode hook coverage, production content workflow, real assets, backend content API, entitlement, analytics, payment, login, video, deploy, DNS, secrets, and NovelHub production infrastructure remain blocked behind separate explicit human approvals.
