<!--
Architecture gate generated by dramadev-architect for Kanban task t_08b6091b.
Scope: docs-only / fake-only / additive planning.
-->

# Phase 5 P1 Q5 Architecture Scope Gate — PWA install education timing default

Status: ARCHITECTURE GATE APPROVE for docs-only / fake-only / additive planning only. Product confirmation remains pending.

## Decision

- Approve the Q5 packet as a docs-only architecture/requirements gate artifact, not as a confirmed product decision. The safe recommended default is: defer PWA install education until after fake unlock success or second visit; do not show PWA install education at the first lock in the default P1 path.
- No runtime architecture, code, fixture, Playwright, unit-test, CI, package, lockfile, manifest, service worker, icon, routing, backend, payment, subscription, login, Facebook API, analytics, database, entitlement, video, deployment, DNS, or secret changes are authorized by this gate.
- This gate does not promote `docs/moboreels/phase-5/p1-q5-pwa-install-education-timing-decision-packet.md` or `docs/moboreels/phase-5/p1-decision-brief.md` from proposal to confirmed product decision. Human product confirmation remains required before treating post-fake-unlock-success or second-visit PWA education as the approved P1 default.
- User/product input is not required to preserve the current P0 architecture and avoid first-lock install education. User/product input becomes required before any implementation, or if anyone proposes first-lock-by-default PWA education, pre-free-preview education, real install/browser/platform behavior, service worker/manifest/icon work, analytics, persistence, legal/compliance/privacy/platform-policy decisions, or brand-significant install claims.

## Source review

- Source of truth: `docs/moboreels/scene-flow-facebook-ad-conversion-prd.md`.
- Secondary source: `docs/moboreels/prototype-b-spec.md`, which delegates implementation detail to the SceneFlow PRD.
- Requirements packet reviewed: `docs/moboreels/phase-5/p1-q5-pwa-install-education-timing-decision-packet.md`.
- PRD §2 lines 20-37 defines the core journey: Facebook Ad -> Watch EP1 -> free episode chain -> first locked episode -> Unlock Drawer -> Unlock EP / Story Pass -> mock purchase/unlock -> return to same episode.
- PRD §3 lines 43-58 forbids Home/Search/Show Detail first, login before free preview, PWA install prompt before first playback, pre-lock recharge/Story Pass promotion, real payment/subscription/Facebook APIs/video, and competitor assets.
- PRD §5.2 lines 86-89 requires free episodes to play without login, recharge, PWA install prompts, or subscription prompts.
- PRD §5.6 lines 130-140 requires post-unlock return to `/variant-b/watch/[showId]?episode=[lockedEpisode]&unlocked=1` and forbids returning Home or losing episode context.
- PRD §6 lines 142-158 preserves the P0 user flow from ad landing through first lock, Unlock Drawer, fake unlock/pass, and same-episode return.
- PRD §7 lines 160-209 requires route context preservation for Watch, locked episode, unlocked return, and Pass routes.
- PRD §8.1 lines 230-237 forbids PWA install prompts before free playback.
- PRD §8.3-§8.4 lines 293-364 defines first locked episode behavior and requires the Unlock Drawer to own the first-lock decision.
- PRD §8.5-§8.6 lines 404-486 requires pass/unlock options and unlock success return to preserve current drama and episode context.
- PRD §9 lines 538-562 says the PWA install prompt must not interrupt first playback, lists possible P1 trigger moments, and requires iOS safe area, bottom drawer safe area, standalone return paths, and weak/offline states to remain distinct from locked state.
- PRD §10 lines 564-610 excludes PWA install rate from P0 success metrics.
- PRD §11-§12 lines 612-649 include no first-playback PWA install prompt in P0 and allow P1 PWA install prompt after high-intent actions.
- PRD §15 lines 744-777 requires QA evidence that EP1 opens, free preview is preserved, no PWA prompt appears during free episodes, first lock opens the Unlock Drawer, fake unlock/pass returns to the same episode, and drawer CTAs remain visible in mobile safe area.

## Architecture scope boundary

- Route boundary: Q5 stays on existing fake-only surfaces: `/variant-b/watch/[showId]` and `/variant-b/pass`. Do not add a real install route, platform settings route, app-store route, Home redirect, Show Detail detour, or source/campaign-specific route branch for PWA education.
- State/data boundary: P0/P1 may continue to derive conversion state from `episode`, `freeEpisodes`, locked/unlocked state, drawer open/closed state, and fake `unlocked=1`. Do not add account state, backend persistence, database records, analytics events, Facebook attribution processing, service worker state, push notification state, wallet ledger, entitlement, or production install state for Q5.
- Timing boundary: Default P1 PWA education must not appear immediately on page load, before EP1 starts, during first free playback, during the free episode chain in a blocking way, at the first lock before/during the Unlock Drawer decision, or as a replacement for the locked-state/Unlock Drawer flow. Safe default timing is only after fake unlock success/same-episode return or in a second-visit fake-only context.
- UI/component boundary: Any future fake-only PWA education must be dismissible, educational, subordinate to the watch/unlock flow, and unable to obscure the first-lock Unlock Drawer primary/secondary/tertiary actions. It must not look like payment, login, subscription, entitlement, offline video, push notification, app-store, or production install functionality.
- Callback/query boundary: Preserve `showId`, `episode`, `unlocked=1`, and safe attribution params when moving between Watch, Pass, fake unlock success, and any future fake-only PWA education surface. A dismiss action must return to or remain on the exact same drama and episode context.
- Documentation boundary: This architecture note is the authorized Q5 docs-only gate artifact. Do not edit the PRD, roadmap, prototype-b-spec, package files, CI, lockfiles, manifest, service worker, icons, tests, fixtures, or source code for Q5 without a separate authorized implementation task and product confirmation.

## P0 invariant preservation

Invariant, quoted exactly:

```txt
/variant-b/watch/[showId]?episode=1&source=facebook -> free episode chain -> first locked episode -> Unlock Drawer -> fake unlock/pass -> same episode with unlocked=1
```

No-regression constraints:

- Facebook-ad users continue to land directly on `/variant-b/watch/[showId]?episode=1&source=facebook`, not Home, Search, Browse, Genre, Show Detail, Pass, login, recharge, install education, or platform/app-store education first.
- Free preview remains prompt-free: no login, recharge, Story Pass prompt, subscription prompt, PWA install prompt, homepage redirect, detail redirect, payment-like CTA, install-like CTA, or real-service dependency before/during free playback.
- The first locked episode remains `freeEpisodes + 1` unless explicitly unlocked by the fake URL flag.
- The first locked episode opens the Unlock Drawer; drawer close leaves the user on the same locked episode; tapping locked playback reopens the drawer.
- The first-lock drawer CTA hierarchy remains primary single-episode unlock, secondary Story Pass, tertiary Maybe Later. PWA education must not become another first-lock CTA in the default P1 path.
- Fake single-episode unlock returns to `/variant-b/watch/[showId]?episode=[episodeNumber]&unlocked=1`, preserving safe attribution.
- Fake Story Pass routes through `/variant-b/pass?story=[showId]&episode=[episodeNumber]` and returns to the same watch episode with `unlocked=1`; it must not return Home, strand users on the Pass page, or lose `episode` context.
- `unlocked=1` remains fake review state only; it is not production entitlement, subscription, login, account, payment, backend, database, wallet, install, or persistent app state.

## Safe fake-only future implementation boundaries

If product later confirms Q5 and a separate implementation task is authorized, keep the implementation fake-only and additive:

1. Post-fake-unlock-success path:
   - Earliest allowed moment: after fake unlock/pass has returned to the same Watch route with `unlocked=1`, or after clearly visible unlock-success feedback on that same episode.
   - The PWA education surface must be dismissible and must leave the user on the same `showId` + `episode` + `unlocked=1` context.
   - It may educate about adding the site to the home screen, but must not invoke real browser install APIs, claim offline playback, imply account persistence, or create entitlement.
2. Second-visit path:
   - Earliest allowed moment: a returning-user fake-only review context after the first-session Facebook-ad P0 funnel has already been preserved.
   - Any local/fake returning-user signal must be explicitly accepted by product/requirements before implementation. Do not introduce backend, login, analytics, cookies, service worker state, or production persistence from this gate.
   - Second-visit education must not redirect first-session ad traffic to Home or change the Watch landing route.
3. Non-default timing experiments:
   - First-lock, after-Maybe-Later, after-two-episodes, follow/continue-watching, or source/campaign-specific timing are not approved defaults here. They require separate product confirmation and acceptance criteria.

## State and timing constraints

- Required state carried through the funnel: `showId`, `episode`, `source`/safe attribution params, lock state, drawer state, and fake unlock state.
- Optional future fake-only state, if separately authorized: a local `pwaEducationDismissed` or `pwaEducationSeen` flag scoped to the browser prototype. It must not be treated as account, analytics, install, or entitlement state.
- Forbidden state from this gate: real user id, login session, backend record, database record, wallet ledger, entitlement, subscription, payment, Facebook event id, CAPI/pixel event, service worker lifecycle state, push permission, production install state, app-store state, or NovelHub infrastructure state.
- Timing guard: first-lock drawer visibility and decision availability must occur before any PWA education can be considered in the default path.
- Timing guard: post-unlock PWA education must not intercept the fake unlock/pass CTA click before same-episode return is visible.
- Timing guard: second-visit education must not be evaluated as part of first-session P0 conversion success.

## Callback and query preservation

Future fake-only PWA education must preserve callback context at these seams:

- Watch free chain: `/variant-b/watch/[showId]?episode=N&source=facebook...` continues through free episodes with attribution intact.
- First locked episode: `/variant-b/watch/[showId]?episode=[freeEpisodes+1]&source=facebook...` opens locked state and Unlock Drawer before install education.
- Fake unlock: `buildWatchEpisodeHref`-equivalent behavior returns to `/variant-b/watch/[showId]?episode=[lockedEpisode]&unlocked=1` and preserves safe attribution before education appears.
- Fake Story Pass: `/variant-b/pass?story=[showId]&episode=[lockedEpisode]...` returns to the same Watch episode with `unlocked=1` and safe attribution before education appears.
- Education dismiss: any dismiss/close action must stay on the same route or rebuild the same Watch href with `showId`, `episode`, `unlocked=1` where applicable, and safe attribution. It must not send users to Home, Search, Browse, Show Detail, external stores, browser settings, or a different episode.

## Mobile safe-area considerations

- Target viewport remains iPhone-like mobile, approximately 390 x 844.
- Future PWA education must account for iOS safe-area inset bottom, home indicator spacing, and bottom drawer stacking.
- It must not cover the Unlock Drawer primary `Unlock EP X` CTA, secondary `Get Story Pass` CTA, `Maybe later`, fake balance/cost, return-to-episode helper copy, or unlocked playback controls.
- It should not compete with the existing bottom sheet z-index/visual hierarchy; first-lock drawer remains the highest-priority conversion surface.
- If presented post-unlock, it should avoid covering the playback area in a way that makes the unlocked episode look like an error, offline, loading, or locked state.
- It must keep weak-network/offline education visually and semantically distinct from locked state and PWA install education.

## QA hooks for later authorized work

No tests are required by this architecture gate. If separate fake-only QA work is authorized later, add only additive checks such as:

- Starting at `/variant-b/watch/[showId]?episode=1&source=facebook`, assert no PWA education appears on page load, before EP1 starts, during first free playback, or through the free episode chain before first lock.
- At first locked episode, assert locked state and Unlock Drawer are visible and PWA education is absent or non-obstructing in the default path.
- Assert drawer primary/secondary/tertiary actions remain visible and tappable in a 390 x 844 mobile viewport with safe-area-sensitive bottom spacing.
- After fake single-episode unlock, assert same-episode route includes `episode=[lockedEpisode]` and `unlocked=1` before any post-unlock PWA education is allowed.
- After fake Story Pass purchase, assert return from `/variant-b/pass` preserves the same story/episode and `unlocked=1` before any post-unlock PWA education is allowed.
- If second-visit education is authorized, assert it appears only in the chosen fake returning-user context and never changes first-session Facebook-ad landing behavior.
- Assert any PWA education copy remains fake/educational and does not imply real installation, offline playback, push notifications, account persistence, analytics, payment, subscription, entitlement, backend, service worker, manifest, or production infrastructure.

## Runtime evidence inspected

- `src/app/variant-b/watch/[showId]/watch-stub.tsx:27-49` parses current Watch query params and builds fake unlock/pass hrefs from current show, episode, and attribution.
- `src/app/variant-b/watch/[showId]/watch-stub.tsx:79-84` resets playback/drawer state when the current episode enters locked state.
- `src/app/variant-b/watch/[showId]/watch-stub.tsx:309-368` renders the first-lock Unlock Drawer with primary fake unlock, secondary Story Pass, and Maybe Later actions.
- `src/lib/query-params.ts:112-139` centralizes fake watch/pass return hrefs while preserving attribution and setting `unlocked=1` for fake unlock/pass returns.
- No PWA education/runtime integration was found or required for this Q5 docs-only gate.

## Deferred infrastructure roadmap

Deferred until separate explicit approval:

1. Product confirmation of Q5 default timing: post-fake-unlock-success, second visit, both, neither, or an alternate experiment.
2. Requirements acceptance note converting the confirmed timing into implementation-ready fake-only criteria.
3. Fake-only UI/UX copy review for educational wording that does not imply real install/offline/account/push behavior.
4. Fake-only implementation and additive QA, only after product confirmation and only within client-side prototype state.
5. Real browser install prompt handling, iOS Add to Home Screen production behavior, manifest/icon/service worker/offline/push work, analytics, install attribution, login/account persistence, backend/database, deployment/DNS/secrets, payment/subscription/entitlement, Facebook APIs, video infrastructure, and NovelHub production infrastructure remain P2+/hard-stop-gated.
6. Legal/compliance/privacy/platform-policy/brand-significant install claims require explicit human/legal/product approval before any production-facing language or platform behavior.

## Task graph and dependencies

1. Product owner gate: approve, reject, or defer the Q5 recommendation. Blocking before implementation.
2. Requirements/docs gate: if approved, write a narrowed fake-only acceptance note with exact trigger timing and copy constraints. Depends on product owner gate.
3. Architecture review gate: confirm state/timing/callback/safe-area constraints and hard-stop exclusions. This document satisfies the current docs-only architecture review for proposal stage.
4. Design/copy gate: draft fake-only education copy and placement rules. Depends on product + requirements gates.
5. Implementation gate: add fake-only client-side PWA education timing if separately assigned. Depends on product + requirements + design/copy gates.
6. QA gate: add/run additive fake-only checks for no pre-playback/free-chain/first-lock prompt, same-episode return before education, dismissibility, safe-area non-obstruction, and copy constraints. Depends on implementation gate.
7. Production infrastructure gate: real install/manifest/service-worker/analytics/backend/legal/platform work remains separate and blocked until explicitly authorized.

## Rejected alternatives

- Show PWA install education at first lock by default: rejected for this default because it competes with the Unlock Drawer decision and first-lock conversion evidence.
- Show PWA install education before or during free preview: rejected because it violates watch-first/free-preview-first requirements.
- Redirect to Home or Show Detail for install education: rejected because it breaks the Facebook-ad Watch landing and same-episode return invariant.
- Use real browser install APIs, service worker, manifest/icon, push, analytics, backend, or persistence for Q5: rejected as out of scope and hard-stop-gated.
- Treat `unlocked=1` as installed-app/account/entitlement state: rejected; `unlocked=1` remains fake review state only.
- Make PWA education copy claim offline playback, push notifications, account persistence, or installed app availability: rejected pending legal/platform/product approval and real infrastructure.

## Open questions

- Does product confirm both post-fake-unlock-success and second visit as co-equal P1 default trigger moments, or should one be preferred first?
- If only one default is selected, should the other remain a later P1 variant or be deferred beyond P1?
- What exact fake-only educational copy is acceptable without implying offline playback, push notifications, real installation, account persistence, entitlement, or production analytics?
- For second-visit fake-only review, what local/fake returning-user signal is acceptable without backend, login, analytics, service worker, cookies, or production persistence?
- If post-unlock education is approved, should it appear after fake unlock-success feedback, after a brief unlocked playback confirmation, or only after the user dismisses any unlock-success toast?
- Should future fake-only QA evidence sample only the post-unlock path, only the second-visit path, or both paths before P1 merge-readiness?

## Risks and gates

- Product decision gate: product must explicitly confirm the timing before implementation. This gate only approves the proposal as architecture-safe.
- Funnel-distraction risk: first-lock install education could dilute the `Unlock EP X` / `Get Story Pass` decision and weaken P0 conversion evidence.
- P0 regression risk: Q5 must not redirect users, require login, show prompts before free preview, interrupt the free chain, skip the drawer, obscure drawer CTAs, or return users away from the same episode after fake unlock/pass.
- Safe-area/viewport risk: any future education surface must not cover drawer CTAs or unlocked playback controls in the target mobile viewport.
- Platform/privacy/legal gate: real install prompts, browser/platform integration, push notifications, service worker/offline claims, consent/data handling, tracking/analytics, or platform policy decisions require explicit human/legal/product approval.
- Data/backend gate: real user persistence, second-visit detection beyond fake-only/local review state, backend APIs, databases, analytics, Pixel/CAPI, or Facebook integrations remain hard-stop-gated.
- Entitlement/security gate: post-unlock education must not confuse `unlocked=1` fake review state with account persistence, entitlement, or installed-app access.
- Implementation gate: any code, fixture, test, CI, browser evidence, service worker, manifest, icon, deployment, or infrastructure work requires a separate authorized task after requirements/product approval.

## Validation performed for this gate

- Read the Q5 decision packet and confirmed it is docs-only / fake-only / non-implementation with product confirmation pending.
- Read the SceneFlow PRD and Prototype B spec sections named above.
- Inspected current fake-only runtime files for route/query preservation, locked drawer timing, drawer CTA hierarchy, and same-episode fake unlock/pass return behavior.
- Confirmed this task requires no runtime/code/test changes before product confirmation.
- No unit, e2e, CI, browser, package, lockfile, deployment, network, secret, backend, payment, subscription, login, analytics, Facebook API, database, entitlement, video, service worker, manifest, icon, real PWA, or NovelHub work was performed.

## Stop/go recommendation

GO for docs-only / fake-only architecture alignment: keep the recommended default as post-fake-unlock-success or second-visit PWA education, with no PWA education at first lock in the default P1 path.

STOP before implementation: product confirmation is required before any fake-only UI, fixture, state, copy, or QA work. Real install/platform/browser/service-worker/manifest/icon/analytics/backend/legal/compliance/privacy/production infrastructure work remains outside this gate and requires separate explicit approval.

## Next action

Product should review Q5 and choose one of: approve both post-fake-unlock-success and second visit as default P1 triggers; approve only one trigger; reject and request a separately scoped first-lock/other timing experiment; or defer Q5 and keep only the current PRD prohibition on first-playback PWA prompts.
