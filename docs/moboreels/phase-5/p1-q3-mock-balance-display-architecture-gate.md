<!--
Architecture gate generated by mandated Claude Code CLI for Kanban task t_e37e46bc.
Session: 38c994f6-b3f6-435a-a075-095f15985efb
Scope: docs-only / fake-only / additive planning.
-->

# Phase 5 P1 Q3 Architecture Scope Gate — mock balance display default

Status: ARCHITECTURE GATE APPROVE for docs-only / fake-only / additive planning only. Product confirmation remains pending.

## Decision

- Approve the drawer-only mock balance display proposal for packaging as a docs-only architecture/requirements gate artifact, not as a confirmed product decision. This authorization covers only docs-only / fake-only / additive planning and a later additive QA review if separately authorized.
- This gate is an architecture packaging approval. It is NOT a product decision: human product confirmation that drawer-only is the P1 default (versus a passive pre-lock balance-chip experiment or deferring Q3 entirely) remains an open gate per `docs/moboreels/phase-5/p1-q3-mock-balance-display-decision-packet.md` and PRD §16 open question on line 785.
- This gate explicitly does NOT authorize production deployment, real payment/subscription/login/Facebook API/analytics/backend/database/entitlement/video infrastructure, secrets, DNS/cutover, licensed/competitor/uncleared assets, legal/compliance/brand-significant monetization or copy decisions, or NovelHub production infrastructure. It does NOT promote the P1 decision brief from proposal to confirmed product decision, and does NOT edit the PRD, roadmap, or prototype-b-spec.
- Architectural note: the existing runtime already conforms to drawer-only balance display. Balance/cost render only inside `data-testid="unlock-drawer-balance"` and `unlock-drawer-cost` (`src/app/variant-b/watch/[showId]/watch-stub.tsx:329-335`); no wallet chip, coin counter, recharge entry, Story Pass prompt, subscription prompt, login prompt, or payment-like CTA is rendered on the Watch page before the first locked episode. The drawer-only default is therefore a no-op preservation default at the architecture layer; any later additive work would only refine fake fixture values or copy, not introduce new pre-lock surfaces.

## Architecture scope boundary

- Route boundary: Q3 stays entirely inside the existing `/variant-b/watch/[showId]` route surface and the existing `/variant-b/pass` mock surface. No new routes, no new query params, no changes to attribution param parsing in `src/lib/query-params.ts`. The Unlock Drawer remains rendered inline inside `src/app/variant-b/watch/[showId]/watch-stub.tsx` and continues to open via the existing first-lock auto-open effect.
- State/data boundary: Mock balance and cost remain read-only fixture data on `SceneFlowShow.mockBalance` and `SceneFlowShow.mockCostPerEpisode` (`src/types/scene-flow.ts:33-34`) populated in `src/data/fixtures/shows.ts`. Optional `mockWalletVariants?: MockWalletVariant[]` (`src/types/scene-flow.ts:16-21,38`) remains optional and unused at runtime unless a separate authorized review introduces it inside the drawer. No new state fields, no new types, no runtime mutation, no storage, no cookies, no network. `unlocked` remains a fake URL flag (`src/lib/lock.ts`); it must not become entitlement, account state, wallet ledger state, or production purchase history.
- UI/component boundary: Balance and cost copy render only inside the drawer at `data-testid="unlock-drawer-balance"` and `data-testid="unlock-drawer-cost"` (`watch-stub.tsx:329-335`). The CTA balance branch (`hasEnoughCoins = show.mockBalance >= show.mockCostPerEpisode`) at `watch-stub.tsx:49` and the primary CTA label switch (`Unlock EP X` vs `Get coins to unlock`) at `watch-stub.tsx:349` are inside the drawer only. Cost helper text on locked rows of the Episode Sheet (`src/lib/episode-sheet.ts:32`) is preserved as PRD §8.2 mandates "Locked episodes showing price or unlock condition"; this is cost-as-unlock-condition on locked rows of a list view, not a pre-lock balance display, and Q3 must not remove or weaken it. Cost and coin pack copy on the Pass page (`src/app/variant-b/pass/pass-stub.tsx:51,56,64-66`) are inside the drawer-driven post-lock flow, not the free preview chain.
- Test boundary: Any later authorized test must be additive under existing `src/lib/*.test.ts` (`episode-sheet.test.ts`, `lock.test.ts`, `query-params.test.ts`, `callback-keys.test.ts`, `episode-range.test.ts`) and the manual-continue regression introduced in `1874f65`. No Playwright additions, no CI/lockfile/package/browser-install changes. Existing P0 manual-continue regression must remain green. Q2 first-lock hook gate work in flight on PR #28 (`15f0cc8`) must not be disturbed.
- Non-scope / hard stops: Real wallet balance, real coin packs, real recharge UI, real payment, real subscription billing, real Story Pass billing, real login, real account/entitlement state, real wallet ledger, real backend/database/content API, real video provider/playback, production analytics, Pixel/CAPI, Facebook API, production deploy/DNS/secrets, licensed/competitor/stock/third-party/public-domain assets, legal/compliance/brand sign-off, pricing/tax/local-price/refund/cancellation/renewal decisions, NovelHub production infrastructure, and other P1 questions (Q1, Q4–Q23) from the P1 brief. Q3 also does not authorize promotion of the P1 brief from proposal to confirmed product decision, nor PRD or roadmap edits.

## P0 invariant preservation

- Invariant, quoted exactly:
  ```txt
  /variant-b/watch/[showId]?episode=1&source=facebook -> free episode chain -> first locked episode -> Unlock Drawer -> fake unlock/pass -> same episode with unlocked=1
  ```
- No-regression constraints:
  - Ad landing route shape, including `?episode=1&source=facebook`, is unchanged; attribution preservation in `appendAttributionParams` (`src/lib/query-params.ts`) is untouched.
  - `freeEpisodes` continues to determine the per-drama lock point via `getEpisodeLockState` (`src/lib/lock.ts`). Q3 must not move, weaken, or bypass the lock point.
  - Free preview comes before login, recharge, subscription, Story Pass promotion, PWA install, balance education, or payment-like CTAs. During the free episode chain on the Watch page, there must be no mock balance chip, no wallet chip, no coin counter, no coin balance text, no recharge entry, no Story Pass prompt, no subscription prompt, no login prompt, no payment-like CTA, and no "Get coins"/"Top up" affordance.
  - The first locked episode opens the Unlock Drawer automatically on first entry (`watch-stub.tsx` drawer auto-open effect); drawer close leaves the page locked; tapping the locked playback area reopens the drawer.
  - Balance and cost remain inside the Unlock Drawer only. They render at `unlock-drawer-balance` and `unlock-drawer-cost` test IDs; they do not appear in the header, the Watch page chrome, the Episode Sheet header, the playback overlay, the episode-complete CTA, or anywhere else on the Watch page during the free preview chain.
  - The Episode Sheet locked-row cost-as-unlock-condition helper (`src/lib/episode-sheet.ts:32`) is preserved unchanged because PRD §8.2 (lines 262–291) requires "Locked episodes showing price or unlock condition". It applies only to locked rows, only inside the Episode Sheet list view, and is not a pre-lock balance display.
  - CTA hierarchy stays: primary `Unlock EP X` / `Get coins to unlock` (balance-conditional, inside drawer only), secondary `Get Story Pass`, tertiary `Maybe later`. Story Pass never becomes primary on the first lock. The balance-conditional primary label switch must not leak balance state outside the drawer.
  - Mock unlock and Pass return continue to land on the same episode with `unlocked=1` via `buildWatchEpisodeHref({..., unlocked: true})` and `buildPassReturnHref` (`src/lib/query-params.ts`). No Home redirect, no episode loss, no attribution drop.
  - Locked state remains visually distinct from network/video error/offline states (PRD §8.3); balance/cost copy must not mention errors, retries, or offline.
  - Fake balance and `unlocked=1` remain review/demo signals only; they never imply real account state, real wallet ledger, real purchase history, real entitlement, real payment availability, or production pricing.

## Later implementation task boundaries

- No implementation should start before human product confirmation on the Q3 default (drawer-only vs. passive pre-lock balance-chip experiment vs. defer). This gate authorizes architecture packaging only.
- If and only if product later confirms drawer-only as the P1 default, surfaces a later authorized dev MAY inspect/change (additive only, fake-only):
  - `src/data/fixtures/shows.ts`: `mockBalance` and `mockCostPerEpisode` values may be tuned per show so balance vs. cost demonstrably exercises both sufficient and insufficient drawer branches; `mockWalletVariants?` may remain unused or be wired into a drawer-only review affordance. Values must remain fake fixtures, original, non-licensed, non-competitor, non-real-pricing.
  - `src/app/variant-b/watch/[showId]/watch-stub.tsx:329-335`: balance/cost copy strings inside the drawer may be refined for clarity (e.g., "Mock balance" / "Mock cost" labels) provided test IDs `unlock-drawer-balance` and `unlock-drawer-cost` remain stable. The balance-conditional CTA branch at `watch-stub.tsx:49,349` may be preserved as-is or wrapped in a tiny pure helper.
  - `src/app/variant-b/pass/pass-stub.tsx:51,56,64-66`: mock cost / mock Story Pass / mock coin pack copy on the Pass route may be aligned with drawer copy for consistency. Copy remains placeholder-only and fake-only. Pass route is reached only after the drawer or a locked-episode entry, so it is not pre-lock.
  - Optional: a new small pure helper (e.g., `resolveDrawerBalanceLabel(show)` or `resolveDrawerCostLabel(show)`) co-located under `src/lib/` with an additive `*.test.ts`. Must be pure, no side effects, no I/O, no environment access.
- If product later authorizes a passive pre-lock balance-chip experiment instead, it must be packaged as a separate decision packet plus a separate architecture gate. It is OUT OF SCOPE for this gate. The pre-lock chip would require explicit constraints (passive, non-CTA, post-free-episode-only, non-recharge, non-Story-Pass-promotional, non-payment-like) that are not approved here.
- Things a later dev MUST NOT change for Q3:
  - `src/lib/query-params.ts` route/attribution shape, `buildWatchEpisodeHref`, `buildPassReturnHref`, `parseWatchQueryParams`, `appendAttributionParams`, or the attribution key set.
  - `src/lib/lock.ts` lock semantics or `getEpisodeLockState` return type.
  - `src/lib/episode-sheet.ts:32` locked-row cost-as-unlock-condition helper (PRD §8.2 requirement). It may be reformatted but must continue to surface cost or unlock condition on locked rows only.
  - `SceneFlowEpisode` / `SceneFlowShow` shape in `src/types/scene-flow.ts` beyond keeping `mockBalance`, `mockCostPerEpisode`, and `mockWalletVariants?` available. No required new fields. No new entitlement field. No new wallet/ledger field.
  - `freeEpisodes`, `firstLockedEpisode`, `episodeRanges`, `totalEpisodes`, or `storyHook` content for `PRIMARY_SHOW_ID` (Q2 first-lock hook work on PR #28 is in flight and must not be disturbed).
  - CTA wiring, ordering, or test IDs in the Unlock Drawer (`unlock-drawer`, `unlock-drawer-hook`, `unlock-drawer-balance`, `unlock-drawer-cost`, `mock-unlock-episode-link`, `story-pass-secondary-link`, `unlock-drawer-maybe-later`).
  - The locked boundary placeholder structure, the drawer auto-open effect, and the locked-playback reopen path in `watch-stub.tsx`.
  - The Watch page chrome, header, playback area, episode-complete CTA, and Episode Sheet header to introduce any pre-lock balance/wallet/coin/recharge/Story Pass/subscription/login/payment-like UI.
  - PRD, roadmap, prototype-b-spec, package/lockfile, CI config, Playwright configuration, environment, secrets, deployment, DNS.

## Required validation for later QA/reviewer

- Read `docs/moboreels/phase-5/p1-q3-mock-balance-display-decision-packet.md` and confirm docs-only / fake-only / non-implementation status remains intact and product confirmation is still pending.
- With dev server local-only (no deploy, no real network), walk the P0 path from `/variant-b/watch/midnight-lantern-oath?episode=1&source=facebook` through the free episode chain to the first locked episode and the drawer:
  - Confirm absence on every free episode page: no `unlock-drawer-balance`-equivalent text, no wallet chip, no coin counter, no coin balance text, no recharge affordance, no Story Pass prompt, no subscription prompt, no login prompt, no "Get coins"/"Top up" CTA, and no payment-like copy in the header, body, or episode-complete CTA. Verify both EP1 and the last free episode (`freeEpisodes`-numbered).
  - Confirm Episode Sheet locked rows still show cost-as-unlock-condition helper from `episode-sheet.ts:32`; this is required by PRD §8.2 and is not a regression. Confirm Episode Sheet does not show balance, wallet, recharge, or payment-like prompts in its header.
  - On reaching the first locked episode, confirm `data-testid="unlock-drawer"` opens automatically and `data-testid="unlock-drawer-balance"` and `data-testid="unlock-drawer-cost"` show fake values sourced from the show fixture only.
  - Confirm balance copy reads as fake/mock (e.g., "Balance: N coins" with mock framing) and does not imply real wallet, real account, real purchase history, real entitlement, or production pricing.
  - Primary CTA reads `Unlock EP X` when `mockBalance >= mockCostPerEpisode` and `Get coins to unlock` otherwise; the `Get coins to unlock` label must not link to or imply a real recharge or payment flow. Secondary CTA reads `Get Story Pass (mock)`; tertiary `Maybe later`.
  - Following `mock-unlock-episode-link` returns to `/variant-b/watch/midnight-lantern-oath?episode=[firstLockedEpisode]&unlocked=1` preserving `source=facebook` and any attribution params.
  - Following `story-pass-secondary-link` then `pass-mock-return-link` lands on the same episode with `unlocked=1`.
- Navigate directly to a later locked episode (e.g., `?episode=10`) without `unlocked=1` and confirm the drawer opens, balance/cost render inside the drawer only, CTA hierarchy is unchanged, and no pre-lock balance UI is revealed in transit.
- Confirm closing the drawer leaves the user on the same locked episode; tapping the locked playback area reopens the drawer; closing does not reveal pre-lock balance UI on the Watch page chrome.
- Confirm balance/cost copy never appears in error/retry/offline states; locked state remains visually distinct from network/video error states.
- Run existing `vitest` unit suites under `src/lib/*.test.ts` (lock, episode-sheet, query-params, callback-keys, episode-range) and confirm all green; the manual-continue regression introduced in `1874f65` must remain green. If a pure helper is added under `src/lib/`, add a co-located `*.test.ts` and run it green.
- Confirm no new network calls, no new env vars, no new imports of payment/auth/analytics/Facebook SDK/video providers, no fixture content sourced from licensed/competitor/third-party material, and no real pricing/tax/local-price/refund/cancellation/renewal copy.
- Capture local-only fake evidence: one screenshot of EP1 free-episode Watch page showing no pre-lock balance UI, one screenshot of the final free-episode Watch page (still no pre-lock balance UI), one screenshot of the first-lock drawer showing `unlock-drawer-balance` and `unlock-drawer-cost`, and one screenshot of the post-unlock same-episode return with `unlocked=1`. All evidence labelled fake/local; no production device, deploy, or analytics implied. Place under `qa-evidence/` consistent with existing conventions.

## Blockers and gates

- Product confirmation gate (open, blocking): product must explicitly confirm one of: (a) drawer-only mock balance display as the P1 default, (b) a separately-scoped passive pre-lock balance-chip experiment with strict non-CTA / post-free-episode-only / non-recharge / non-Story-Pass-promotional / non-payment-like constraints, or (c) defer Q3 and keep only the existing PRD requirement that balance/cost appear inside the Unlock Drawer. No implementation work begins until this returns.
- Editorial/copy gate (open, blocking before any drawer copy refinement): approve fake-balance and fake-cost copy phrasing so it cannot be mistaken for real wallet/account/purchase state; confirm no licensed/competitor/uncleared text.
- Monetization/legal gate (hard stop): any real balance, coin pack, wallet ledger, payment, subscription, pricing, tax, refund, cancellation, renewal, or local-price behavior requires explicit human/legal/product approval and is out of scope for Q3.
- Entitlement/security gate (hard stop): fake balance and `unlocked=1` must remain review/demo signals and must never become production entitlement or account state.
- Data/backend gate (hard stop): real wallet/account state, backend APIs, databases, analytics, Pixel/CAPI, or Facebook integrations remain hard-stop-gated.
- Infrastructure gate (hard stop): production deploy, DNS/cutover, secrets, NovelHub production infrastructure, real video provider, and real login remain hard-stop-gated.
- Concurrency gate: Q2 first-lock hook gate work (PR #28, head `15f0cc8`) and PR #27 manual-continue regression (head `1874f65`) are open and clean; Q3 must not introduce edits that touch those surfaces or cause merge conflicts with them.

## Recommended next Kanban tasks

1. Product owner task (blocking gate): confirm or reject drawer-only mock balance display as the P1 default per `docs/moboreels/phase-5/p1-q3-mock-balance-display-decision-packet.md`. If approving a pre-lock balance-chip experiment instead, scope it as a separate decision packet with its own architecture gate; that path is NOT approved here.
2. Editorial/copy task (blocking gate, docs-only): approve fake-balance and fake-cost copy phrasing for the drawer so it is unambiguous mock framing; confirm no licensed/competitor/uncleared text.
3. Requirements task (gated on 1 + 2, docs-only): narrow the Q3 decision packet into a fake-only implementation-ready acceptance note for dev/QA, preserving the P0 invariant verbatim and the no-pre-lock-prompt list.
4. Dev task (gated on 1 + 2 + 3, additive, fake-only): if drawer-only is confirmed, optionally tune `mockBalance` / `mockCostPerEpisode` in `src/data/fixtures/shows.ts` to exercise both sufficient and insufficient drawer branches, and optionally refine drawer balance/cost labels in `watch-stub.tsx:329-335` without changing test IDs. No type/route/lock changes. Coordinate with the Q2 first-lock hook work on PR #28 to avoid conflicts in shared files.
5. QA task (gated on 4): additive fake-only regression that verifies pre-lock absence across the full free episode chain, drawer presence and balance/cost rendering at the first locked episode, drawer CTA hierarchy under both sufficient and insufficient balance states, Episode Sheet locked-row cost-as-unlock-condition preservation, and the full P0 route → drawer → unlock/pass → same-episode `unlocked=1` return on `midnight-lantern-oath`. Add to `src/lib/*.test.ts` only if a pure helper was introduced; otherwise manual walkthrough plus existing unit suites. Local/fake-only evidence labelled accordingly.
6. Reviewer task (gated on 4 + 5): verify P0 invariant preserved verbatim, no hard-stop scope crossed, no PRD/roadmap edits, no real-service dependencies introduced, no pre-lock balance/wallet/coin/recharge/Story Pass/subscription/login/payment-like UI added, and evidence labelled fake-only.
7. Hard-stop holdback: real wallet ledger, real coin packs, real recharge, real payment, real subscription, real Story Pass billing, real login, real account/entitlement, real backend/database/content API, production analytics, Pixel/CAPI, Facebook API, production deploy/DNS/secrets, licensed/competitor/uncleared assets, legal/compliance/pricing/tax/refund/cancellation/renewal decisions, and NovelHub production infrastructure remain blocked behind separate explicit human approvals.
